\lstset{
    numbers=left,                % line numbers on the left
    numberstyle=\tiny,           % style of line numbers
    showstringspaces=false,      % do not make spaces visible
    stepnumber=1,                % show every line number
    numbersep=5pt,               % gap between numbers and code
    backgroundcolor=\color{white}, % background color
    keywordstyle=\color{blue},   % keyword style
    basicstyle=\ttfamily\small,  % code font and (optionally) size
    frame=single,                % add a frame around the code
    breaklines=true,             % break long lines
    breakindent=0pt,             % don't indent broken part of lines
    linewidth=4.7in,             % max line length
    breakatwhitespace=false      % breaks anywhere
}
\chapter{Listings}\thispagestyle{empty}
