\StrLen{$SUBDIR}[\sectionnamelength]
% "./" is the only possibility for length 2, the repository root
\def\sectionname{Root Directory}
\ifthenelse{\sectionnamelength > 2}{}{\def\sectionname{Subdirectory $SUBDIR}}
\section{\sectionname}
